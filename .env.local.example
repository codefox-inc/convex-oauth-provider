# Deployment used by `npx convex dev`
# Run `npx convex dev` to automatically populate this file
CONVEX_DEPLOYMENT=dev:your-deployment-name # team: your-team, project: your-project

# Backend URLs (automatically set by `npx convex dev`)
CONVEX_URL=https://your-deployment.convex.cloud
CONVEX_SITE_URL=https://your-deployment.convex.site

# Vite requires VITE_ prefix for client-side access
# Copy the CONVEX_URL value here for the React frontend
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# OAuth Provider Configuration (set via Convex Dashboard or CLI)
# If using Convex Auth (recommended): JWT_PRIVATE_KEY and JWKS are auto-generated
# If NOT using Convex Auth: set these manually
#   npx convex env set OAUTH_PRIVATE_KEY "-----BEGIN RSA PRIVATE KEY-----..."
#   npx convex env set OAUTH_JWKS '{"keys":[...]}'
#
# Required: Your application's public URL
#   npx convex env set SITE_URL http://localhost:5173
#
# Optional: Allowed CORS origins (localhost is automatically allowed)
# For production, add external services that need to access your OAuth endpoints:
#   npx convex env set ALLOWED_ORIGINS https://claude.ai,https://chatgpt.com,https://example.com --prod
